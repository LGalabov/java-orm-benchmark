-- MySQL bootstrap for java-orm-benchmark
-- The ormbench database and bench user are created by environment variables.
-- Schema tables are created at runtime by SchemaManager in the common module.
SELECT 1;
